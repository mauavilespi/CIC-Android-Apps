<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.themap.intro_fragments">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_intro"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_intro_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.IntroFragments">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.IntroFragments.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>